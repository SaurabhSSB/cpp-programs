# include <iostream>
# include <vector>
using namespace std;

int main() {

    // Declare empty vector
    vector<int> vc;

    // Declare and initialize with value
    vector<int> vc2= {11, 12, 14, 16};

    // Declare with a fixed size and default value
    vector<int> vc3 (5,10); // Size is 5 and all elements are intialized by 10.

    return 0;
}
